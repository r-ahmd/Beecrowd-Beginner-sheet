#include <stdio.h>
 
int main() 
{
    int n, position, max=0;
    for(int i=1; i<=100; i++)
    {
        scanf("%d", &n);
        if(n>max)
        {
            max=n;
            position=i;
        }
    }
    printf("%d\n%d\n",max, position);
 
    return 0;
}
