#include <stdio.h>
 
int main() 
{
    int x, arr[10];
    scanf("%d", &x);
    
    for(int i=0; i<10; i++)
    {
        arr[i]=x;
        printf("N[%d] = %d\n", i, x);
        x+=x;
    }
    
    return 0;
}
