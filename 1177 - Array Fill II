#include <stdio.h>
 
int main() 
{
    int arr[1000], n;
    scanf("%d", &n);
    
    for(int i=0, j=0; i<1000; i++, j++)
    {
        if(n==j)
            j=0;
        printf("N[%d] = %d\n", i, j);
    }
 
    return 0;
}
