#include <stdio.h>
 
int main() 
{
    int x;
    while(1)
    {
        int sum=0;
        scanf("%d", &x);
        if(x==0)
            break;
        if(x%2!=0) x++;
        for(int i=1; i<=5; i++, x+=2)
            sum+=x;
        printf("%d\n", sum);
    }
    
    return 0;
}
