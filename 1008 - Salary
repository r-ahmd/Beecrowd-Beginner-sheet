#include <stdio.h>
 
int main() 
{
    double b, c, d;
    int a;
    scanf("%d", &a);
    scanf("%lf%lf", &b, &c);
    
    d = b * c;
    
    printf("NUMBER = %d\n", a);
    printf("SALARY = U$ %.2lf\n", d);
 
    return 0;
}
