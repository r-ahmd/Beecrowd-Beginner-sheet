#include <stdio.h>
 
int main() 
{
    int n;
    scanf("%d", &n);
    float a, b, c, avg;
    while(n--)
    {
        scanf("%f %f %f", &a, &b, &c);
        avg= (a*2)+(b*3)+(c*5);
        avg /= 10;
        printf("%.1f\n", avg);
    }
 
    return 0;
}
