#include <stdio.h>
#include <math.h>

int main() 
{
 
    double a1, b1, a2, b2, c;
    
    scanf("%lf%lf%lf%lf", &a1, &b1, &a2, &b2);
    
    c = sqrt ((a2 - a1) * (a2 - a1) + (b2 - b1) * (b2 - b1));
    
    printf("%.4lf\n", c);
    
    
    return 0;
}
