#include <stdio.h>
 
int main() 
{
    int arr[100];
    double n;
    scanf("%lf", &n);
    arr[0] = n;
    printf("N[0] = %.4lf\n", n);
    for(int i=1; i<100; i++)
        printf("N[%d] = %.4lf\n", i, n/=2);
    
 
    return 0;
}
