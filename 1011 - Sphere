#include <stdio.h>
 
int main() 
{
    double r, pi, a;
    
    pi=3.14159;
    
    scanf("%lf", &r);
    
    a = pi * (r*r*r) * (4.0/3);
    
    printf("VOLUME = %.3lf\n", a);
    
    return 0;
}
